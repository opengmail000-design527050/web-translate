# Changelog

## 0.2.0

- 页面跳转、切换配置和重翻时，作废并中止旧的在途请求；旧结果不再串进新页面或新缓存。
- 同一页面开多个标签页时，缓存改为由后台增量合并，不再互相覆盖。
- 429 限流遵从 `Retry-After`，同一接口共享冷却时间，段落最多自动重试 5 次。
- 为后台消息增加 120 秒兜底，避免 MV3 service worker 意外回收后并发槽永久卡住。
- 监听全局设置变化，所有已开启翻译的标签页都能同步配置。
- 支持单页应用复用 DOM 节点和无限滚动移除旧节点，不再留下旧译文或持续占用单元上限。
- 会改变译文的服务地址、推理参数写法、自定义提示词等设置进入缓存签名。
- 首次安装自动打开设置页。
